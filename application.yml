server:
  port: 8765

spring:
  application:
    name: Consumer


  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false


eureka:
  client:
    service-url:
      defaultZone:  http://10.10.10.40:8761/eureka/
  instance:
    prefer-ip-address: true